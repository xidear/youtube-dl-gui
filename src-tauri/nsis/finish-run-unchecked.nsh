; Make the "Run application" checkbox on the finish page unchecked by default.
; This avoids the app launching right after install (which can freeze for a while on first run).
!define MUI_FINISHPAGE_RUN_NOTCHECKED
